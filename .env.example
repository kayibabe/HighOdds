# Never commit live credentials. Populate these in Railway or a local .env file.
DATABASE_URL="postgresql://highodds:highodds@localhost:55432/highodds?schema=public"
API_FOOTBALL_KEY=""
AUTH_SECRET="replace-with-a-long-random-secret"
AUTH_RESEND_KEY=""
EMAIL_FROM="HighOdds <noreply@example.com>"
ADMIN_EMAIL="owner@example.com"
# Only needed to run `npm run db:seed-admin --workspace=@highodds/db`; not read at runtime.
ADMIN_PASSWORD=""
API_FOOTBALL_DAILY_QUOTA=7500
API_FOOTBALL_QUOTA_SAFETY_PERCENT=90
